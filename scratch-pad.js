/**
 * Created by dias on 15-07-22.
 */
var str = '1,2,3,GO'

var len = str.length

var lastElement = str.substr(len -1)

console.log(lastElement)

if (str === str.toUpperCase()){
    console.log('All Caps')
}